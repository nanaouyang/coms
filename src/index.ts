import { Debounce } from "./debounce";
import { Regex } from "./regex";
import { Tree } from "./tree";
import { Type } from "./type";
import { Img } from "./Img";

const coms = {
  Debounce,
  Regex,
  Tree,
  Type,
  Img,
};
export default coms;
export { Tree, Type, Regex, Debounce, Img };
